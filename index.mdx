---
title: "Introduction"
description: "TypeScript SDK for x402r refundable payments on EVM chains"
---

# Welcome to x402r SDK

Build refundable payment flows with escrow protection on Ethereum and EVM-compatible chains.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Get started with x402r SDK in minutes
  </Card>
  <Card title="Installation" icon="download" href="/installation">
    Install the SDK in your project
  </Card>
  <Card title="SDK Guide" icon="book" href="/sdk/client">
    Learn about the client and action groups
  </Card>
  <Card title="API Reference" icon="code" href="/api/payment/authorize">
    Explore the complete API documentation
  </Card>
</CardGroup>

## What is x402r?

x402r is a refundable payments protocol that enables escrow-protected transactions on EVM chains. The SDK provides a type-safe TypeScript interface for building payment flows with built-in refund mechanisms, dispute resolution, and configurable fee structures.

### Key Features

<CardGroup cols={2}>
  <Card title="Escrow Protection" icon="shield-check">
    All payments include an escrow period during which refunds can be processed
  </Card>
  <Card title="Multi-Chain Support" icon="layer-group">
    Works on Ethereum, Base, Polygon, Arbitrum, Optimism, and more with CREATE3 addresses
  </Card>
  <Card title="Type-Safe Client" icon="check-double">
    Fully typed TypeScript client with action groups organized by domain
  </Card>
  <Card title="Role-Based Presets" icon="users">
    Pre-configured clients for payer, merchant, and arbiter roles
  </Card>
  <Card title="Evidence System" icon="file-lines">
    Submit and retrieve evidence for payment disputes
  </Card>
  <Card title="Extensible Architecture" icon="puzzle-piece">
    Extend the client with custom action groups and plugins
  </Card>
</CardGroup>

## Quick Example

```typescript
import { createPublicClient, createWalletClient, http } from 'viem'
import { baseSepolia } from 'viem/chains'
import { createX402r } from '@x402r/sdk'

const publicClient = createPublicClient({ 
  chain: baseSepolia, 
  transport: http() 
})

const walletClient = createWalletClient({ 
  chain: baseSepolia, 
  transport: http() 
})

const x402r = createX402r({
  publicClient,
  walletClient,
  operatorAddress: '0x…',
})

// Get payment state
const state = await x402r.payment.getState(paymentInfo)

// Request a refund
const txHash = await x402r.refund.request(paymentInfo, amount, nonce)
```

## Supported Chains

The SDK supports all major EVM chains with consistent CREATE3 contract addresses:

- Ethereum Mainnet & Sepolia
- Base & Base Sepolia
- Polygon
- Arbitrum One
- Optimism
- Celo
- Avalanche C-Chain
- Linea
- Monad

## Next Steps

<CardGroup cols={3}>
  <Card title="Quickstart Guide" icon="play" href="/quickstart">
    Build your first refundable payment
  </Card>
  <Card title="Action Groups" icon="layer-group" href="/sdk/action-groups">
    Explore available operations
  </Card>
  <Card title="Examples" icon="code" href="/examples/basic-payment">
    View real-world usage examples
  </Card>
</CardGroup>
